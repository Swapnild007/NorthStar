name: Deploy NorthStar to GitHub Pages
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Validate JavaScript
        run: |
          node --check app.js
          python - <<'PY'
          import pathlib
          s=pathlib.Path("data/curriculum.js").read_text()
          stack=[]; pairs={")":"(","]":"[","}":"{"}; opens=set(pairs.values()); in_str=None; esc=False; line=1; i=0
          while i<len(s):
              ch=s[i]
              if ch=="\\n": line+=1
              if in_str:
                  if esc: esc=False
                  elif ch=="\\": esc=True
                  elif ch==in_str: in_str=None
              elif ch in "'\"`": in_str=ch
              elif ch in "([{": stack.append((ch,line))
              elif ch in ")]}":
                  if not stack or stack[-1][0]!=pairs[ch]: raise SystemExit(f"delimiter mismatch at line {line}: {ch}; stack top={stack[-1] if stack else None}")
                  stack.pop()
              i+=1
          print("delimiter stack:",stack[-10:])
          PY
          node --check data/curriculum.js
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: .
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4